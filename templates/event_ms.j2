---
title_en: "{{ title_en }}"
title_tr: "{{ title_tr }}"
name: "{{ name }}"
type: "ms"
date: "{{ datetime.split('T')[0] }}"
time: "{{ datetime.split('T')[1] }}"
duration: "{{ duration }}"
location_en: "{{ location_en }}"
location_tr: "{{ location_tr }}"
---

## Abstract

{{ description_en }}

---

## Özet

{{ description_tr }}
